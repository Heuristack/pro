#!/bin/bash
CODEHOME=$HOME/pro/code
N=$1
NAME=$2
hpp=$CODEHOME/src/main${N:-0}.hpp
[ -f $hpp ] && cp $hpp ${NAME:-main}.hpp
cpp=$CODEHOME/src/main${N:-0}.cpp
[ -f $cpp ] && cp $cpp ${NAME:-main}.cpp

